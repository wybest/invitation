function queryString(name)
{
   var reg = new RegExp("(^|&)" + name + "=([^&]*)(&|$)", "i");
   var r = window.location.search.substr(1).match(reg);
   if (r != null) return (r[2]);
   return null;
} 
function removeSpace(s)
{
   //alert(s);
   return s.toString().split(' ').join('').split('　').join('');
}
function isNull(o){
	return (typeof o).toString()=="undefined";
}
function getHref(){
	var h=window.location.href;
	return h.substring(0,h.lastIndexOf("/")+1);//返回包括最后一个斜杠在内的网址
}
<!-- custom name-->
var guestName=queryString("guestName")?decodeURIComponent(queryString("guestName")):''